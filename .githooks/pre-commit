#!/bin/sh
# Rewrite cache-busting hashes in all HTML files before each commit.
python3 bust.py

# Re-stage any HTML files that bust.py just modified
git diff --name-only -- '*.html' | xargs -r git add
