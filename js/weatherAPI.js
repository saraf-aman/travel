/**
 * weatherAPI.js
 * Future: fetches live weather for a trip location.
 * Activated when trip JSON has weather.apiEnabled = true.
 */
export async function getWeather(lat, lon) {
  // Placeholder — wire up to Open-Meteo or similar free API
  console.log(`Weather fetch for ${lat},${lon} not yet implemented`);
  return null;
}
